## Context / Backlog item(s)
- Implement V3 counter read endpoints (Global + Single User) (M3, E3.1)

## Scope
- Added `GET /api/v3/counter/{userId}` endpoint.
- Validated existing `GET /api/v3/counter` endpoint consistency.
- Verified `ICounterReadRepository` and `SqlCounterReadRepository` implementation.

## Verification
- `.\scripts\test.ps1` (Result: Passed)
- Manual verification of API endpoints via `Invoke-RestMethod` (Result: Success)

## Notes / Risks
- None. `read.UserCounters` table was removed in M1, using direct Event Store query as per architecture.

## Manual steps required
- None
