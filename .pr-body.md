## Backlog item(s)
- Add Asp.Versioning v3 route group conventions (Milestone: M0, Epic: E0.1)

## Scope
- Replaced `Asp.Versioning.Mvc` with `Asp.Versioning.Http` for Minimal API support
- Configured API versioning with Header and QueryString readers (default v3.0)
- Created versioned route group `/api/v3` using `NewVersionedApi` and `HasApiVersion`
- Refactored `EndpointExtensions.MapEndpoints` to work with `IEndpointRouteBuilder` for route group support
- Updated `HealthTests` to verify `/api/v3/health` endpoint

## Verification
- `dotnet build` succeeded
- `dotnet test` succeeded (all 11 tests passing)

## Notes / Risks
- Changed from `UrlSegmentApiVersionReader` to `HeaderApiVersionReader` + `QueryStringApiVersionReader` to support hardcoded `/api/v3` path
- Default API version is now 3.0 (was 1.0)
- Removed `WithApiVersionSet` due to compatibility issues with .NET 10 preview - using `HasApiVersion` instead

## Manual steps required
- None
