## Backlog item(s)
- EPIC: Add EF Core persistence with write/read schemas and migrations (Milestone: M1, Epic: E1.1)

## Scope
- Added `Microsoft.EntityFrameworkCore.SqlServer` and `Microsoft.EntityFrameworkCore.Design` packages.
- Configured LocalDB connection string in `appsettings.Development.json`.
- Implemented `TheButtonDbContext` with `write` and `read` schemas.
- Defined entities: `CounterIncrementedEvent`, `IdempotentCommand`, `UserCounter`.
- Created initial EF migration with a custom SQL injection for the `read.GlobalCounter` view.
- Implemented dev-only auto-migration in `Program.cs`.
- Updated `DOC-05-persistence-design.md` with implementation details (surrogate key for idempotency).

## Verification
- `dotnet build` succeeded.
- `.\scripts\test.ps1` succeeded (no regressions in V2).
- Manual: Database and tables verified to exist after `dotnet ef database update`.

## Notes / Risks
- Converted `IdempotentCommand` to use a surrogate `Id` (BIGINT) as the Primary Key to allow `UserId` to be `NULL` for global increments while maintaining a unique index across `(Operation, UserId, IdempotencyKey)`.

## Manual steps required
- None (already completed during execution).
