## Backlog item(s)
- Restore V2 endpoints integration tests (keep V2 behavior stable) (Milestone: M0, Epic: E0.3)

## Scope
- Re-implemented `POST /api/v2/counter` using Minimal APIs (replaces deleted Controller).
- Restored `CounterApiTests.cs` from git history.
- Enabled `UrlSegmentApiVersionReader` in `Program.cs` to support URL-based versioning for V2.

## Verification
- `dotnet build`
- `dotnet test` (Passed)

## Notes / Risks
- `UrlSegmentApiVersionReader` was added to `Program.cs` to support `/api/v2/...` route versioning. V3 defaults to 3.0 via header/query or hardcoded route, so it remains unaffected, but good to verify V3 still works (though V3 endpoints aren't implemented yet beyond health).
- This restoration ensures the mobile client (using V2) will not break when we deploy.

## Manual steps required
- None
