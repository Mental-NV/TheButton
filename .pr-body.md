## Backlog item(s)
- Add scripts/test.ps1 and scripts/test.sh (Milestone: M0, Epic: E0.2)

## Scope
- Created `scripts/test.ps1` for consistent local and CI test execution on Windows.
- Created `scripts/test.sh` for consistent local and CI test execution on Linux/macOS.
- Both scripts run `dotnet build` and `dotnet test` and exit with 0 on success.

## Verification
- Ran `.\scripts\test.ps1` locally:
  - All projects built successfully.
  - All 11 tests passed.
  - Script exited with "All tests passed!" message.

## Notes / Risks
- None.

## Manual steps required
- None.
