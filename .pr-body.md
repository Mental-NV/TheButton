## Backlog item(s)
- Minimal API mapping skeleton (feature-based) (Milestone: M0, Epic: E0.1)

## Scope
- Replaced `AddControllers` with `AddEndpoints` extension for Minimal APIs.
- Added `IEndpoint` and `EndpointExtensions` for auto-scanning using Clean Architecture slice pattern.
- Updated `Program.cs` to use endpoint mapping.
- Added sample feature `Features/Health` to verify mapping path.

## Verification
- `dotnet build` succeeded
- `dotnet test` succeeded (Added HealthTests, Removed CounterApiTests which tested removed controllers)

## Notes / Risks
- This is a structural change; existing Controllers (should be none in this refactor, but standard template ones might be removed) are gone.
- Health check available at `/health`.

## Manual steps required
- None
